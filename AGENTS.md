# 목적

이 저장소는 경희대학교 중앙 IT 동아리 쿠러그(KHLUG)의 동아리 시스템인 사이트(SIGHT)를 개발하기 위한 통합 워크스페이스다.

프론트엔드와 백엔드 등 관련 코드 저장소를 이 저장소 안에 각각 clone하여, AI 에이전트가 여러 저장소에 걸친 요구사항을 한 번에 파악하고 설계, 구현 및 검증할 수 있도록 한다. 또한 저장소 간 Task와 공통 규칙, 정책 및 표준을 이곳에서 통합 관리한다.

# 워크스페이스 구성

`sight-workspace`를 먼저 clone한 뒤 작업에 필요한 코드 저장소를 그 안에 clone한다. 일반적인 구성은 다음과 같다.

```text
sight-workspace/
├── sight-spring-backend/
├── sight-frontend/
├── knowledge/
├── tasks/
├── AGENTS.md
└── README.md
```

- 하위 코드 저장소는 각각 독립적인 Git 저장소로 유지한다.
- 하위 코드 저장소를 `sight-workspace` 저장소의 커밋에 포함하지 않는다.
- 사이트 코드의 커밋과 Pull Request는 해당 코드 저장소에서 관리한다.
- 여러 저장소에 걸친 Task와 공통 개발 컨텍스트는 `sight-workspace`에서 관리한다.

# 지침 적용 원칙

- 관련 작업을 진행할 하위 저장소의 파일을 조사하거나 변경하기 전에, 해당 저장소의 `AGENTS.md`를 처음부터 끝까지 읽고 따른다.
- 하위 저장소의 `AGENTS.md`가 작업에 따라 추가 규칙 문서를 읽도록 지시하면, 해당 문서도 작업을 진행하기 전에 처음부터 끝까지 읽는다.
- 이 문서는 워크스페이스 전체의 통합 작업에 적용한다.
- 하위 저장소의 구현 방식, 테스트, 커밋 등에 관한 구체적인 규칙은 해당 저장소의 지침을 따른다.
- 여러 저장소의 규칙이 서로 다르면 각 변경 사항에는 그 파일이 속한 저장소의 규칙을 적용한다.
- 저장소 간 계약이나 작업 범위에 영향을 주는 결정은 통합 Task에 일관되게 반영한다.

# Task 관리 원칙

- 여러 저장소에 걸친 작업은 `tasks/` 아래의 통합 Task를 기준으로 추적한다.
- Task에는 관련 저장소, 저장소별 변경 범위, 저장소 간 의존 관계와 검증 방법이 드러나야 한다.
- 구현 중 요구사항이나 설계가 변경되면 관련 코드와 Task 문서 사이에 모순이 남지 않도록 함께 갱신한다.
- 단일 저장소에만 적용되는 세부 Task 규칙이 해당 저장소에 있다면 그 규칙도 함께 따른다.
- Backend Task 문서를 새로 만들거나 수정하기 전에 `tasks/_standard/backend.md`를 처음부터 끝까지 읽고 따른다.
- 새 Backend Task 문서는 YAML frontmatter에 `type: backend`를 선언하고 `tasks/_schema/backend.yaml`의 입력 schema와 `tasks/_standard/backend.md`의 작성 지침을 따라 `tasks/open/` 아래에 만든다.
- Task 문서를 새로 만들거나 수정한 뒤에는 저장소 root에서 현재 OS와 architecture에 맞는 `tools/task-lint/bin/task-lint-*` binary를 실행한다.
- 현재 환경에 맞는 binary가 없거나 source hash가 일치하지 않으면 저장소 root에서 다음 명령을 실행하고 lint를 다시 실행한다.

```text
docker buildx build --file tools/task-lint/Dockerfile --output type=local,dest=tools/task-lint/bin .
```

- Task lint가 성공해야 Task 문서 작업을 완료한 것으로 간주한다.
- `tools/task-lint/bin/` 아래의 생성된 binary는 수정하거나 Git에 포함하지 않는다.

# Knowledge 파일 관리 원칙

`knowledge/` 하위 파일은 최소한으로 유지한다.

다음 중 하나에 해당할 때만 파일을 분리한다.

- 파일 내용이 500라인을 초과할 때
- 명확히 다른 범위의 내용일 때

그 외에는 하나의 파일 안에서 섹션으로 구분한다.

# 기능 개발 순서

새로운 기능을 구현하거나 기존 기능을 변경할 때는 다음 순서로 진행한다. 기능 개발이 목적이 아니라면 작업 성격에 맞는 절차를 적용한다.

1. 사용자 요구사항과 영향을 받는 저장소를 명확히 정의한다.
2. 요구사항을 바탕으로 API, 데이터 구조, 요청 흐름 및 저장소 간 계약을 설계한다.
3. 통합 Task에 저장소별 구현 범위와 의존 관계를 정리한다.
4. 프론트엔드와 백엔드 구현을 각 저장소의 규칙에 따라 진행한다.
5. 저장소별 테스트와 저장소 간 연동을 검증한다.
6. 구현 결과를 리뷰하여 요구사항, 설계, 공통 정책 및 각 저장소의 개발 원칙을 준수하는지 확인한다.
7. 필요한 수정을 반영하고 다시 검증한다.
8. 완료된 변경과 검증 결과를 사용자에게 간략히 보고한다.

에이전트 역할을 분리할 수 있는 환경에서는 요구사항 분석, 설계, 프론트엔드 개발, 백엔드 개발 및 리뷰 역할을 작업 특성에 맞게 활용한다.
