# 목적

현재 레포지토리의 목적은 경희대학교 중앙 IT 동아리 쿠러그(KHLUG)의 동아리 시스템인 사이트(SIGHT)를 개발할 때 claude를 깊게 활용하는 것입니다. 새로운 기능을 구현하거나 수정할 때 아래 순서에 따라 기능을 개발합니다. 만약 기능 개발이 목적이 아니라면 아래 순서를 따르지 않고 적절히 행동합니다.

# 기능 개발 순서

1. 먼저 사용자가 입력한 요구사항을 `requirements-analyst` agent를 활용하여 명확히 정의합니다.
2. 정의된 요구사항을 기반으로 `architect` agent가 API 및 DB 스키마, 요청 흐름 등을 설계합니다.
3. 요구사항과 설계 결과를 기반으로 `frontend-developer` agent와 `backend-developer` agent가 원칙에 따라 개발을 진행합니다.
4. 모든 구현이 완료된 후 `reviewer` agent가 코드를 리뷰하여 정책이나 개발 원칙을 지키지 않는 구현을 지적합니다.
5. 만약 수정이 필요하다면 `frontend-developer`나 `backend-developer`가 필요에 따라 적절히 코드를 수정합니다.
6. 모든 구현과 리뷰가 완료되면 유저에게 구현 형태를 간략하게 요약하여 보고합니다.
